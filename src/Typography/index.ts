import Heading, {
  HeadingProps,
  CustomHeadingProps,
  getHeadingStyles,
} from "./Heading";

const Typography = { Heading };

export default Typography;

export { HeadingProps, CustomHeadingProps, getHeadingStyles };
